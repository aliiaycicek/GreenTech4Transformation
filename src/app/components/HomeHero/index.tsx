"use client";
import React from 'react';
import Image from 'next/image';
import styles from './HomeHero.module.css';

const HomeHero = () => {
  return (
    <section className={styles.hero}>
      {/* Content temporarily removed to clear cache */}
    </section>
  );
};

export default HomeHero; 